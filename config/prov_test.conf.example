# PROV 测试环境配置文件
# 仅用于测试环境，生产环境不使用此文件
#
# 使用方法：
#   1. 复制此文件为 prov_test.conf
#   2. 修改 uid 值为实际的 ECU UID
#
# 文件位置（按优先级）：
#   - /etc/tbox/prov_test.conf（绝对路径）
#   - ./config/prov_test.conf（相对路径，开发环境推荐）
#
# 格式: key=value
# 键: uid, 值: ECU UID 串（8-32位）

uid=0123456789ABCDEF
